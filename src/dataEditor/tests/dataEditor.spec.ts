import { describe } from 'mocha'
import assert from 'assert'
import { DataEditor } from '../core/editor/dataEditor'
import { MockIniter } from './utils/fixtures'

describe('Data Editor Abstraction Behavior', () => {
  it('Is required to contain a UI component', () => {})
  // it("Should not be publically constructable", () => {
  //   const editor = DataEditor.Create(new MockIniter)
  // })
})
